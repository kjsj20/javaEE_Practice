<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Cmmt">
	<select id="selectAll" parameterType = "int" resultType = "Cmmt">
		select * from cmmt where board_id = #{board_id} order by cmmt_id;
	</select>
	
	<insert id="insert" parameterType = "Cmmt">
		insert into cmmt(board_id, msg, author) values(#{board_id}, #{msg}, #{author});
	</insert>
</mapper>